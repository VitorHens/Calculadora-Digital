# Contribuindo

Este projeto faz parte do meu portfólio acadêmico. Sugestões podem ser enviadas por issues ou pull requests.

Boas práticas:

- não versionar a pasta `vendor/`;
- instalar dependências com `composer install`;
- manter commits curtos e descritivos;
- não publicar credenciais ou arquivos `.env`.
